# 本地开发指南（Windows / pnpm）

本文档说明如何在本仓库本地配置环境变量（.env）并启动前后端进行开发调试。

## 前置条件

- Windows 10/11
- Node.js 22（仓库根目录 [.npmrc](file:///d:/code/nocodb/.npmrc) 指定了 `use-node-version`）
- pnpm（建议 v10+）
- MySQL 8.x（或兼容版本），本地可连通 `localhost:3306`

## 环境变量（.env）配置

本项目会从各自 package 目录读取 `.env` 文件。

### 1）后端（packages/nocodb）

1. 复制示例文件：

```powershell
Copy-Item .\packages\nocodb\.env.example .\packages\nocodb\.env
```

2. 编辑 `.env`：

- `NC_DB`：后端元数据库连接（示例使用 MySQL）
- `NC_PORT`：后端端口（默认 8080）
- `NC_AUTH_JWT_SECRET`：本地开发用的 JWT 密钥

示例（与 `.env.example` 一致）：

```dotenv
NC_DB=mysql2://localhost:3306?u=root&p=123456&d=nocodb_meta_dev
NC_PORT=8080
NC_AUTH_JWT_SECRET=local-dev-secret
```

说明：

- `d=` 指定数据库名。建议本地开发使用单独数据库（例如 `nocodb_meta_dev`），避免与已有环境冲突。

### 2）前端（packages/nc-gui）

1. 复制示例文件：

```powershell
Copy-Item .\packages\nc-gui\.env.example .\packages\nc-gui\.env
```

2. 确认后端地址：

```dotenv
NUXT_PUBLIC_NC_BACKEND_URL=http://localhost:8080
```

### 3）安全与提交

仓库已在 [.gitignore](file:///d:/code/nocodb/.gitignore) 中忽略 `.env` / `.env.*`，避免把本地账号密码误提交。

## 安装依赖

在仓库根目录执行：

```powershell
pnpm install
```

如果你在国内网络环境，推荐使用 npm 镜像源以提升依赖下载速度。仓库根目录 [.npmrc](file:///d:/code/nocodb/.npmrc) 已设置：

```text
registry=https://registry.npmmirror.com
```

## 启动服务

### 1）启动后端

```powershell
pnpm run start:backend
```

验证后端是否正常：

- 版本接口：`http://localhost:8080/api/v1/version` 返回 200 即可

### 2）启动前端

```powershell
pnpm run start:frontend
```

前端默认地址：

- `http://localhost:3000/`

## 常见问题

### 1）迁移锁导致后端启动失败

如果你看到类似报错：

- `Migration table is already locked`

可以执行解锁脚本（读取 `packages/nocodb/.env` 内的 MySQL 连接）：

```powershell
node .\scripts\unlock-meta-mysql.js
```

### 2）元数据库“脏库”导致迁移报表已存在

如果你看到类似报错：

- `Table 'xxx' already exists`

通常是之前启动中断导致的元数据库不完整。开发环境最省事的方式是重置元数据库：

```powershell
node .\scripts\reset-meta-mysql.js
```

然后重新启动后端即可。

### 3）type-check 内存溢出（Windows）

如果你在后端启动日志里看到：

- `FATAL ERROR: ... heap out of memory`
- `[type-check] Issues checking service aborted - probably out of memory`

可以通过环境变量控制 rspack 的类型检查进程内存上限，或直接关闭类型检查：

```powershell
$env:NC_TS_CHECKER_MEMORY_LIMIT="4096"
pnpm run start:backend
```

或：

```powershell
$env:NC_DISABLE_TYPECHECK="true"
pnpm run start:backend
```

对应实现位于 [rspack.dev.config.js](file:///d:/code/nocodb/packages/nocodb/rspack.dev.config.js)。

